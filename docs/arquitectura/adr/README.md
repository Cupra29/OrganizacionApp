# Registro de decisiones de arquitectura (ADR)

Formato: contexto → decisión → alternativas consideradas → consecuencias.
Un ADR aceptado **no se edita**: se reemplaza por otro que lo supere.

| ADR | Decisión | Estado | Reversible |
|---|---|---|---|
| [001](./ADR-001-stack-y-monorepo.md) | TypeScript de extremo a extremo, monorepo pnpm, Fastify + React | aceptado | Sí |
| [002](./ADR-002-persistencia-postgresql.md) | PostgreSQL con `tstzrange` y constraints de exclusión; Drizzle | aceptado | Parcialmente |
| [003](./ADR-003-modelo-temporal-y-zonas-horarias.md) | **Jornada como unidad; UTC + zona IANA contextual** | aceptado | **No** |
| [004](./ADR-004-motor-determinista-vs-llm.md) | Motor determinista; LLM solo en los bordes | aceptado | Sí |
| [005](./ADR-005-recurrencia-y-excepciones.md) | **RRULE + generador CYCLE, excepciones por instante original** | aceptado | **No** |
| [006](./ADR-006-versionado-de-plan-y-diff.md) | **Instantáneas inmutables; linaje asignado al colocar; diff de dos niveles** | aceptado | **No** |
| [007](./ADR-007-entrevista-formulario-progresivo.md) | Formulario progresivo con puertas; JSONB parcial | aceptado | Sí |
| [008](./ADR-008-sincronizacion-calendarios.md) | Publicación en calendario separado de solo lectura | aceptado | Sí |
| [009](./ADR-009-alcance-primer-entregable.md) | Flujo completo sobre un subconjunto de variantes | aceptado | Sí |
| [010](./ADR-010-autenticacion.md) | Acceso sin contraseña por enlace de un solo uso | aceptado | Sí |
| [011](./ADR-011-privacidad-por-diseno.md) | **Sin campos de salud; reducción a restricción temporal** | aceptado | **No** |
| [012](./ADR-012-estrategia-de-despliegue.md) | Contenedor único en PaaS, Postgres gestionado | aceptado | Sí |
| [013](./ADR-013-motor-como-funcion-pura.md) | **El motor no conoce base de datos, red ni reloj** | aceptado | **No** |
| [014](./ADR-014-cumplimiento-rgpd.md) | RGPD como techo; narrativas estructuradas, no texto redactado | aceptado | Parcialmente |
| [015](./ADR-015-parametros-de-calibracion.md) | Fricción conservadora (15 % + 7 min) y corrección del tope emergente | aceptado | Sí |
| [016](./ADR-016-version-de-typescript.md) | TypeScript 6.0, no el compilador nativo 7.0, hasta que exista la API programática | aceptado | Sí |
| [017](./ADR-017-determinismo-del-ics.md) | El `.ics` es función de la versión del plan; `packages/ical` no lee el reloj | aceptado | Sí |
| [018](./ADR-018-expansion-de-recurrencia-sin-rrule.md) | Expansión de recurrencia propia sobre `Temporal`; `rrule` fuera del núcleo, `rrule-temporal` solo de oráculo | aceptado | Sí |

Los cinco marcados como no reversibles (003, 005, 006, 011, 013) son puertas de una sola
dirección: cambiarlos después implica migración destructiva o reescritura del núcleo. Son los
que merecen discusión antes de escribir código. ADR-014 es parcialmente irreversible: el
cambio de narrativas afecta a datos ya escritos, por eso se decidió antes de la fase 0.

## Revisiones registradas

| Fecha | Qué pasó |
|---|---|
| 2026-07-27 | Q1, Q4 y Q5 resueltas confirmando sus supuestos. |
| 2026-07-27 | Q12 (equipo y plazo) resuelta: **una persona, sin plazo**. **ADR-001 reexaminado y confirmado** — ver su §5. |
| 2026-07-27 | Q2 resuelta: marcado explícito del anclaje. Amplió el enum de ADR-003 a **tres** valores. |
| 2026-07-27 | Q8 resuelta: RGPD como techo. Nace **ADR-014** y cambia el esquema de `sacrifices`, `plan_diffs` e `infeasibility_reasons`. |
| 2026-07-28 | Q3, Q7, Q9, Q10 y Q11 resueltas confirmando sus supuestos. Q11 gana un argumento mejor: es consecuencia de la regla nº6, no una preferencia. |
| 2026-07-28 | Q6 resuelta con fricción conservadora. Nace **ADR-015**, que además **corrige una afirmación falsa de Q5** sobre el tope emergente. |
| 2026-07-28 | ✅ **Ninguna pregunta abierta. La fase 0 puede arrancar.** |
| 2026-07-28 | Los 15 ADRs pasan de `propuesto` a **`aceptado`**. A partir de aquí ninguno se edita: se supera con otro. |
| 2026-07-28 | Convenciones de [07](../07-convenciones-propuestas.md) aplicadas al `CLAUDE.md` de la raíz. Repositorio git inicializado. |
| 2026-07-28 | **Disponibilidad confirmada en 10–20 h semanales**, último dato de entrada que quedaba sin confirmar. Cae dentro de la franja para la que el plan está dimensionado: ADR-001 y ADR-009 se mantienen sin cambios. Nota fechada en ADR-001 en vez de ADR de reemplazo, porque ninguna decisión cambia. |
| 2026-07-29 | Nace **ADR-016** al ejecutar la fase 0: TypeScript 7.0 salió estable sin API programática y `dependency-cruiser` no lo soporta. Concreta la versión que ADR-001 dejaba abierta; no lo contradice. |
| 2026-07-29 | Cerrados los cuatro cabos de la fase 0. **Licencia MIT** declarada. `web-solo-contracts` y `apps-no-se-cruzan` **demostradas** (no hacía falta esperar a la fase 7). El disparador de ADR-016 pasa de nota a **workflow mensual** que comprueba sus dos condiciones. **Dependabot** para `github-actions`, que cierra el hueco abierto al anclar las acciones por SHA. Solo queda diferido `pull_request`, condicionado a que haya colaboradores. |
| 2026-07-29 | Entregada la primera parte de la fase 1, el **guardrail de reloj y aleatoriedad**. Nace **ADR-017**: `packages/ical` entra en su alcance porque el `.ics` de una versión debe ser reproducible byte a byte. **Ningún ADR queda reemplazado** — ninguno hablaba de `DTSTAMP`; es una concreción, como lo fue ADR-016. Corrige además la afirmación que motivó la revisión: un `DTSTAMP` cambiante **no** duplica eventos, la deduplicación es por `UID`. El [05](../05-plan-de-implementacion.md) se corrige en el mismo movimiento: nombraba `noRestrictedGlobals`, que es incapaz de la precisión que el propio documento exige. |
| 2026-07-29 | Nace **ADR-018** al evaluar la dependencia de recurrencia de la fase 1: la expansión se implementa sobre `Temporal` y **`rrule` no entra en producción** (devuelve `Date` cuyo significado depende de la zona del proceso — invisible para `dependency-cruiser`, para el guardrail y para un CI en UTC). `rrule-temporal` queda como **oráculo diferencial** en tests. **Ningún ADR queda reemplazado**: ADR-005 no nombraba biblioteca; esto concreta su §1 y §3 y **enumera el subconjunto**, que hasta hoy no era una especificación. Corrige además un **criterio de aceptación insatisfacible** del [05](../05-plan-de-implementacion.md): un 4×3 es un ciclo de 7 días, así que produce semanas civiles idénticas, no distintas. Descubre que el guardrail no ve `Temporal.Now` ni la zona ambiente. Abre **Q13** y **Q14**. |
| 2026-07-29 | **Q13 y Q14 resueltas** el mismo día que se abrieron. **Q14 confirma** ADR-018 §3: sin `BYDAY` posicional, la fase 1 no crece. **Q13 no confirmó el supuesto**: el turno rotativo real está **desalineado** de la semana civil, así que la fixture representativa pasa a ser un ciclo desalineado y **la demo de la fase 3 cambia de patrón** — decía "4×3", que es alineado, y habría enseñado ocho semanas idénticas como primera recompensa visible del proyecto. **ADR-005 recibe una nota fechada, no un reemplazo**: su justificación de `CYCLE` por "ciclo desfasado de la semana civil" pasa de hipotética a empírica y ninguna decisión cambia — mismo mecanismo que la confirmación de disponibilidad en ADR-001. La longitud exacta del ciclo real sigue sin fijarse. **⚠ La parte de Q13 de esta fila quedó desmentida el 2026-07-30 — ver la última fila.** |
| 2026-07-29 | `qa-engineer` audita los criterios de aceptación de la fase 1 **antes de que exista código** y encuentra seis defectos, todos confirmados: una **property test tautológica** (`sueño + vigilia == nextWake − wake` es álgebra, la satisface una jornada de longitud cero), dos criterios infra-especificados, uno **satisfacible por accidente** y dos entregas sin ningún criterio. Corregidos en el [05](../05-plan-de-implementacion.md). **ADR-018 recibe una nota fechada, no un reemplazo**: su ejemplo del turno de 720 min "el día del cambio de horario" no cruzaba la transición y por tanto no discriminaba; la decisión (minutos reales sobre la línea de instantes) no cambia. Al escribir la propiedad que sustituye a la tautología apareció un **defecto en el pseudocódigo de [03 §3.1](../03-motor-de-planificacion.md)**: `wakeSig` usaba la zona de `d` y no de `d+1`, así que con un viaje las jornadas dejaban de embaldosar la línea de tiempo. Se fijan además las **zonas de referencia para fixtures** en [07 §4.E](../07-convenciones-propuestas.md) — `America/Mexico_City` no vale para cambio de horario desde 2022. |
| 2026-07-29 | Cerrada la laguna de `franjaQueContiene` que `qa-engineer` dejó marcada en [03 §3.2](../03-motor-de-planificacion.md): presuponía que un hueco cabe en una sola franja de energía, y `calcularHuecos` nunca corta por franjas. **Un hueco pasa a tener un perfil de energía segmentado en vez de un `tier` único**, y la colocación elige `(hueco, instante)` en vez de solo hueco — sin eso, un pico de 22:00–01:00 dentro de una tarde libre era estructuralmente inalcanzable y el cronotipo no se cumplía. **Sin ADR**: no contradice ninguna decisión vigente, hace total una función que ya se presuponía total, y [04 §5](../04-contratos-api.md) ya modelaba `slots` con un `tier` uniforme cada uno. **ADR-015 no se mueve**: las restricciones duras siguen midiendo el hueco, así que las plazas de colocación son las mismas; se descartó trocear el hueco precisamente porque sí las habría movido. **ADR-011 §2 decidió** que un `capacity_modifier` `NONE` no corta disponibilidad: ahí la indisponibilidad es un `FixedCommitment` y esto es capacidad de foco. Corrige además que el arrastre degradaba el hueco entero en vez de su ventana. |
| 2026-07-29 | Cerrado el segundo cabo de la segmentación: **el arrastre es `min(nivel, LOW)` y no se compone**, en vez de un decremento encadenado. Lo decide [02 §4](../02-modelo-de-datos.md), que definió la variante y dice "degrada a **`LOW`**" — `degradar(PEAK)` daba `NEUTRAL`, así que los dos documentos solo coincidían cuando la base ya era `NEUTRAL`. Y el decremento **no cumplía su propósito declarado**: con `NEUTRAL = +1` en la puntuación, el motor seguiría colocando foco profundo justo después de una clase, que es lo que el arrastre existe para impedir. Efecto secundario que desaparece: tres arrastres solapados llegaban a `SIN_FOCO`, indistinguible de un `capacity_modifier` `NONE` (ADR-011 §2) y **descontando capacidad por acumulación accidental**. Ahora `tierEn` es el ínfimo de cotas independientes: idempotente, conmutativo e inmune al orden de iteración. **ADR-015 no se mueve.** Anotada además deuda preexistente: los seis pesos `W_*` y la tabla `valor` **no están en `EngineInput.params`** pese al límite nº 5, y la segmentación les sube el apalancamiento; se recomienda **ADR nuevo al calibrarlos** en la fase 4. |
| 2026-07-30 | Entra el primer código de la fase 1 (`eaf92f2`): el módulo único de `Temporal` y la ampliación del guardrail. **ADR-018 §9 recibe nota fechada, no reemplazo.** Lo implementado prohíbe **más** que lo que el §9 enumeraba, y solo en esa dirección: `resolvedOptions` en lectura amplia (la forma literal se esquiva partiendo la expresión en dos sentencias) y **`globalThis` entero**, que el §9 no mencionaba pero que **no es decisión nueva** — mecaniza el descarte de parchear `globalThis.Temporal` que el propio ADR-018 ya había escrito en "Alternativas consideradas". Atajar el receptor destapó dos formas que nadie había enumerado: `globalThis.crypto` y **`globalThis.process.env.TZ`, el modo de fallo exacto por el que ADR-018 descartó `rrule`** — seguía abierto dentro del núcleo. ADR-018 §1 pasa de convención a **regla mecanizada** (`polyfill-temporal-solo-en-su-modulo`). Queda anotado un cabo: con `globalThis` prohibido, el "sale en una línea" del día que `Temporal` sea nativo **no puede ser la línea obvia**. [07 §4](../07-convenciones-propuestas.md) actualizado contra el código. |
| 2026-07-30 | **Llega el dato exacto de Q13 y desmiente su resolución del 29.** El turno real es un **2-2-3 de ciclo de 14 días**: 14 es múltiplo de 7, así que está **enganchado** a la semana civil con **periodo 2**, no desfasado. La respuesta archivada decía "desalineado" porque **la pregunta agrupaba mal** —"desalineado (2-2-3, 8 días…)"— dos regímenes que no son el mismo: 8 días deriva un día por ciclo, 14 alterna entre dos patrones. **La nota de ADR-005 del 29 se reescribe, no se apila otra encima**: afirmaba que la justificación de `CYCLE` pasaba a ser empírica por el desfase, y la cláusula correcta es la **ergonómica** —el ADR nombra el 2-2-3 de 14 días como "directamente hostil" desde el 2026-07-24, y ese resultó ser el turno real, así que la confirmación es genuina pero por otra frase—. **La decisión de `CYCLE` no se toca.** El 2-2-3 entra como tercera fixture de la fase 1 (caza una trampa propia: reducir el ciclo módulo 7 lo colapsa a un patrón semanal, equivocado pero plausible) y sostiene la demo de la fase 3. La de 8 días **no se toca**, porque el 29 se escribió como elección de prueba y no como dato del usuario. Queda el número que faltaba: el periodo es `L / mcd(L, 7)`, y lo que refuta la semana plantilla es **periodo ≥ 2**, no la deriva. |
| 2026-07-30 | Cerrada la etapa 2 (`c718e06`) y con ella el **residuo transmeridiano** que [03 §3.1](../03-motor-de-planificacion.md) había dejado abierto. Con un override hacia el este, `sleep < wakeSig` **es falsa** y el sueño salía negativo — pero el síntoma escondía el daño: el intervalo `[wake, sleep)` **se salía de la jornada e invadía la siguiente**, así que `brutoAsignable` contaba esos minutos **dos veces**. Se acota `sleep` a `wakeSig` (necesidad estructural, no preferencia) y se conserva `recorteVigilia` como evidencia, que es lo único que permite distinguir "te acostaste tarde" de "cruzaste husos". **No es `INFEASIBLE`** —nadie sacrificó sueño, el usuario cogió un vuelo— sino el caso extremo de `SLEEP_DEBT`, con la maquinaria que ya existía. **ADR-003 recibe nota fechada, no reemplazo**: su fórmula `nextWakeAt − sleepAt` se hace total; las tres reglas no se tocan, y la decisión que produce el caso —el sueño es siempre local, sin anclaje— se reexamina y se mantiene. Se fija además cómo `zonaEfectivaEn` cruza una fecha civil con un `during` de instantes (inicio del día civil en la zona base, lo único que la hace función pura de `d`), se corrige la firma de la etapa 2 en el [05](../05-plan-de-implementacion.md) —recibía la zona efectiva ya resuelta, con lo que los tres valores de `anchor` eran indistinguibles— y se deja de prometer que el `Finding SLEEP_DEBT` nace en `packages/temporal`: la jornada expone la evidencia y lo emite F2. |
| 2026-07-30 | Tercera aparición del **mismo defecto de forma**: un número plausible escrito sin la condición que lo hace cierto. Los "17 h de salto México → Lord Howe" de la nota anterior solo valen en **verano austral**; en agosto son 16,5 h y el recorte es 510, no 540. Lo encontró `engine-dev` al implementar y **paró en vez de ajustar el número al documento**. Al corregirlo apareció que el ejemplo de Madrid del mismo párrafo tenía el defecto idéntico: `sueño = 0` solo en verano europeo, 60 min en enero. Los cuatro casos quedan tabulados **con fecha** y los cuatro van a la suite. Nace la convención en [07 §4.E](../07-convenciones-propuestas.md): **un offset no es propiedad de una zona, sino de una zona en una fecha**, y cuando una fixture depende del salto entre dos zonas van las **dos temporadas**. Los antecedentes de la misma forma: "4×3 con ciclo desfasado" (sobrevivió en tres documentos desde el 2026-07-24) y "necesita un ciclo no múltiplo de 7". |
| 2026-07-31 | Entregada la etapa 1 (`1dfb408`). **ADR-018 recibe dos notas fechadas, ninguna de reemplazo.** (1) `UNTIL` **no tenía dueño**: el §4 exige compararlo como instante y el §1 decide que la etapa 1 no tiene instantes ni zonas, así que las dos secciones se contradecían en apariencia. Queda escrito que la etapa 1 recibe siempre un **límite de fecha civil** y que la reducción de `UNTIL` a esa fecha ocurre en el módulo de zonas, **exacta y no conservadora**. Verificado el argumento de monotonía y añadidas tres precisiones que no estaban: el día civil de `UNTIL` se calcula **en la zona de la regla y no en UTC**, la monotonía es **débil** y con eso basta, y el argumento **aguanta los cambios de línea de cambio de fecha** porque al avanzar el reloj más de 24 h desaparece la fecha civil intermedia. (2) El **oráculo diferencial del §8 tiene dominio de validez**: `rrule-temporal` diverge a propósito en el ancla no sincronizada —lo que da **evidencia empírica** a la alternativa que el §6 ya había descartado— y en RFC 5545 §3.3.10, donde recorta en vez de omitir **y arrastra el recorte**, así que a partir de febrero difieren todas las ocurrencias. Un oráculo sin dominio declarado se silencia entero la primera vez que molesta. |
