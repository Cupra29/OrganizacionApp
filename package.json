{
  "name": "organizacionapp",
  "private": true,
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@11.17.0",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "typecheck": "pnpm -r --no-bail run typecheck",
    "lint": "biome check .",
    "format": "biome check --write .",
    "test": "vitest run",
    "test:engine": "vitest run --project @oa/engine",
    "depcruise": "depcruise packages apps --config .dependency-cruiser.cjs",
    "depcruise:cobertura": "depcruise packages apps --config .dependency-cruiser.cjs --output-type json | node scripts/verificar-cobertura-grafo.mjs",
    "guardrail:cobertura": "node scripts/verificar-guardrail-nucleo.mjs",
    "verify": "pnpm run typecheck && pnpm run lint && pnpm run test && pnpm run depcruise && pnpm run depcruise:cobertura && pnpm run guardrail:cobertura"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "dependency-cruiser": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
