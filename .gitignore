# Dependencias
node_modules/
.pnpm-store/

# Build
dist/
build/
*.tsbuildinfo

# Entorno — nunca versionar secretos ni datos personales
.env
.env.*
!.env.example

# Tests
coverage/

# Caches
.turbo/
.cache/
*.local

# Memoria de agentes — estado derivado, la fuente de verdad está en docs/arquitectura/.
# Sin anclar a propósito: el directorio se crea junto al cwd de la sesión, no en la raíz,
# así que `.claude/agent-memory/` a secas solo casaría en la raíz y dejaría escapar el resto.
**/.claude/agent-memory/

# Editor / SO
.DS_Store
.vscode/
.idea/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
